name: Nightly Build
on:
  workflow_dispatch:

jobs:
  builds:
    name: Build & Release
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Deps
        run: |
          sudo apt-get install -y gcc-mingw-w64 libgl1-mesa-dev libxi-dev libxcursor-dev libxrandr-dev libxinerama-dev libwayland-dev libxkbcommon-dev

      - uses: actions/setup-go@v5
        with:
          go-version: "1.23.1"

      - name: Build
        run: |
          mkdir output
          go build -v -o output/datpack_linux --ldflags="-s -w -X 'main.GitCommitHash=$(git rev-parse --short=8 HEAD)'" cmd/datpack/*.go
          echo "Linux: datpack"
          go build -v -o output/datunpack_linux --ldflags="-s -w -X 'main.GitCommitHash=$(git rev-parse --short=8 HEAD)'" cmd/datunpack/*.go
          echo "Linux: datunpack"
          go build -v -o output/modelviewer_linux --ldflags="-s -w" cmd/modelviewer/*.go
          echo "Linux: modelviewer"
          go build -v -o output/roomviewer_linux --ldflags="-s -w" cmd/roomviewer/*.go
          echo "Linux: roomviewer"
          go build -v -o output/scrviewer_linux --ldflags="-s -w" cmd/scrviewer/*.go
          echo "Linux: scrviewer"
          go build -v -o output/t32viewer_linux --ldflags="-s -w" cmd/t32viewer/*.go
          echo "Linux: t32viewer"
          go build -v -o output/timviewer_linux --ldflags="-s -w" cmd/timviewer/*.go
          echo "Linux: timviewer"
          go build -v -o output/tm3pack_linux --ldflags="-s -w -X 'main.GitCommitHash=$(git rev-parse --short=8 HEAD)'" cmd/tm3pack/*.go
          echo "Linux: tm3pack"
          go build -v -o output/tm3unpack_linux --ldflags="-s -w -X 'main.GitCommitHash=$(git rev-parse --short=8 HEAD)'" cmd/tm3unpack/*.go
          echo "Linux: tm3unpack"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/datpack_win.exe --ldflags="-s -w -X 'main.GitCommitHash=$(git rev-parse --short=8 HEAD)'" cmd/datpack/*.go
          echo "Windows: datpack"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/datunpack_win.exe --ldflags="-s -w -X 'main.GitCommitHash=$(git rev-parse --short=8 HEAD)'" cmd/datunpack/*.go
          echo "Windows: datunpack"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/modelviewer_win.exe --ldflags="-s -w" cmd/modelviewer/*.go
          echo "Windows: modelviewer"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/roomviewer_win.exe --ldflags="-s -w" cmd/roomviewer/*.go
          echo "Windows: roomviewer"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/scrviewer_win.exe --ldflags="-s -w" cmd/scrviewer/*.go
          echo "Windows: scrviewer"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/t32viewer_win.exe --ldflags="-s -w" cmd/t32viewer/*.go
          echo "Windows: t32viewer"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/timviewer_win.exe --ldflags="-s -w" cmd/timviewer/*.go
          echo "Windows: timviewer"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/tm3pack_win.exe --ldflags="-s -w -X 'main.GitCommitHash=$(git rev-parse --short=8 HEAD)'" cmd/tm3pack/*.go
          echo "Windows: tm3pack"
          CGO_ENABLED=1 CC=x86_64-w64-mingw32-gcc GOOS=windows GOARCH=amd64 go build -v -o output/tm3unpack_win.exe --ldflags="-s -w -X 'main.GitCommitHash=$(git rev-parse --short=8 HEAD)'" cmd/tm3unpack/*.go
          echo "Windows: tm3unpack"

      - name: Create Release and Upload Assets
        uses: ncipollo/release-action@v1.14.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
          tag: nightly
          prerelease: true
          name: nightly
          artifacts: "output/*"

  del_runs:
    runs-on: ubuntu-latest
    steps:
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 30
          keep_minimum_runs: 3
